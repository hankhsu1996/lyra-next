/// Preprocessor placeholder.
///
/// Will handle `` `define ``, `` `ifdef ``, `` `include `` etc.
/// For now, passes tokens through unchanged.
pub fn preprocess(tokens: Vec<lyra_lexer::Token>) -> Vec<lyra_lexer::Token> {
    tokens
}
